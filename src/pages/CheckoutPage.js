import { PageHero } from '../components';
import styled from 'styled-components';

const Checkout = () => {
  return (
    <mian>
      <PageHero title="checkout" />
      <Wrapper className="page">hello</Wrapper>
    </mian>
  );
};

const Wrapper = styled.div``;

export default Checkout;
